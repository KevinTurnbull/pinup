= link_to "/venues/#{venue.id}/edit", class: 'venue col-md-12' do
  %strong= venue
  - unless venue.address.nil?
    %p
      %small= venue.address
  - unless venue.city.nil?
    %p
      %small= venue.city
